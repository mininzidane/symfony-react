import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  continueCta: {
    marginTop: 20,

    '&.has-extra-margin': {
      marginTop: 45,
      marginBottom: 10,
    },
  },
}));
