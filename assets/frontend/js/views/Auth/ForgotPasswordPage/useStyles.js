import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  wrapper: {
    padding: [[87, 0, 85]],
  },
  root: {
    margin: 'auto',
    display: 'flex',
    justifyContent: 'center',
  },
}));
