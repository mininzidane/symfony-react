import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  form: {
    textAlign: 'left',
  },
  field: {
    height: 53,
  },
  submitButton: {
    marginTop: 40,
  },
}));
