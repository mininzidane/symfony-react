import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  root: {
    marginLeft: 'auto',
    fontSize: 12,
    lineHeight: '18px',
    textAlign: 'right',
  },
}));
