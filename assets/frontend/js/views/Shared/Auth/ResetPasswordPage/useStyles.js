import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  form: {
    textAlign: 'left',
    maxWidth: 470,
    margin: 'auto',
  },
  field: {
    height: 53,
  },
  submitButton: {
    marginTop: 40,
  },
}));
