import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  form: {
    textAlign: 'left',
    maxWidth: 470,
    margin: 'auto',
  },
  submitButton: {
    marginTop: 14,
    marginBottom: 20,
  },
}));
