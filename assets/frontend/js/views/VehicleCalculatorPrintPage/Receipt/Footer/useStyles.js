import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  root: {
    marginTop: 14,
    display: 'flex',
    justifyContent: 'space-between',
    fontSize: 12,
    lineHeight: '18px',
  },
}));
