import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  root: {
    fontSize: 11,
    lineHeight: '16px',
    marginTop: 12,
    color: '#828282',
    textAlign: 'center',
  },
}));
