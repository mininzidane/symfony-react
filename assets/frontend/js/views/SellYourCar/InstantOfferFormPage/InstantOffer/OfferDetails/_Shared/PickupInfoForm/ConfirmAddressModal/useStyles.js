import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  root: {
    overflow: 'auto',
  },
  body: {
    minHeight: 'auto',
  },
  footer: {
    backgroundColor: '#F6F6F6',
    border: 'none',
    paddingTop: 0,
    paddingBottom: 20,
  },
}));
