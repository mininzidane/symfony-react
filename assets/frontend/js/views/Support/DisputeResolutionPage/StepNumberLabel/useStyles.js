import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  root: {
    width: '100%',
    fontSize: 14,
    lineHeight: '16px',
    fontWeight: 700,
    color: 'rgba(0, 0, 0, 0.5)',
  },
}));
