import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  root: {
    fontSize: 28,
    lineHeight: '40px',
    fontWeight: 300,
    color: '#000000',
    margin: 0,
  },
}));
