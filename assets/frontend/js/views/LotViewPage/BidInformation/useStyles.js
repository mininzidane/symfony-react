import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  bidContainer: {
    marginTop: 12,
  },
  bidNow: {
    paddingTop: 0,
    paddingBottom: 0,
  },
  cardBody: {
    position: 'relative',
  },
}));
