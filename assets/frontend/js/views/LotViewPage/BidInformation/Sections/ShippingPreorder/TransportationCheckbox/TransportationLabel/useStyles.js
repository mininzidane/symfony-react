import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  wrap: {
    marginRight: 4,
  },
  tooltip: {
    display: 'inline-block',
    position: 'relative',
    top: '-1px',
  },
}));
