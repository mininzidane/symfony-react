import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  root: {
    backgroundColor: '#E6ECFD',
    borderRadius: '6px',
    marginLeft: 14,
    marginRight: 14,
  },
}));
