import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  root: {
    borderRadius: '4px',
    backgroundColor: '#E6ECFD',
  },
  modalBody: {
    backgroundColor: '#fff',
  },
}));
