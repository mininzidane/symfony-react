import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  tag: {
    padding: '4px 12px',
    borderRadius: '4px',
    background: '#E0E0E0',
  },
  message: {
    color: '#828282',
  },
}));
