import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  root: {
    padding: 0,
    maxWidth: 420,
    margin: '-20px auto 0px',
  },
  footer: {
    textAlign: 'center',
    marginTop: 15,
  },
}));
