import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  root: {
    display: 'grid',
    gridGap: 4,
    textAlign: 'center',
  },
  button: {
    fontSize: '13px !important',
  },
  btn: {
    marginTop: 8,
  },
}));
