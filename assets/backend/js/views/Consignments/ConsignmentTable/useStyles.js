import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  tableContainer: {
    paddingBottom: '70px',
    minHeight: '360px',
  },
  emptyState: {
    minHeight: '360px',
  },
}));
