import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  sortWrap: {
    display: 'flex',
    justifyContent: 'flex-end',
    flexWrap: 'wrap',
    marginBottom: 15,
  },
}));
