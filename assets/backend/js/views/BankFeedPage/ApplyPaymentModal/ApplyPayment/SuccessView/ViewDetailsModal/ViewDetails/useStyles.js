import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  loader: {
    minHeight: 315,
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  },
}));
