import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  root: {
    width: 16,
    height: 16,
    overflow: 'hidden',
    display: 'inline-block',
    marginLeft: 5,
  },
}));
