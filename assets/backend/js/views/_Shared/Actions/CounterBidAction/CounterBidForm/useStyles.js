import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  input: {
    marginBottom: '10px',
  },
  submitWrapper: {
    display: 'flex',
    alignItems: 'center',
    marginBottom: '10px',
  },
  minValue: {
    marginRight: '10px',
  },
}));
