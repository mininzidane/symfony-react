import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  currentBid: {
    background: '#CD0000',
    color: 'white',
    padding: '0 3px',
  },
  right: {
    minWidth: 130,
  },
}));
